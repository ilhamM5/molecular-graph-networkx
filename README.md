# molecular-graph-networkx
**Creators:**  
- Ilham Mneimneh  
- Mohamad El Nahas

### About the Notebook

The Jupyter notebook utilizes `mol` and `sdf` files to build iterable graphs via networkx representing chemical structures of molecules. The test data represents those of small-molecule drugs. The created graphs are then utilized to manually calculate graph indices utilized in early stages of drug discovery, visualized, and then validated using the networkx package.

---

### Running the Notebook

For a quick test run, you can try it out [here](https://mybinder.org/v2/gh/ilhamM5/molecular-graph-networkx/0ee5d082371529a4100147094526f5175499596a?urlpath=lab%2Ftree%2FSubmission.ipynb)

To get started:

1. **Run all cells** in the notebook.
2. Navigate to the **last cell** to launch and interact with the graphical user interface (GUI).
3. All non-essential cells are initially hidden to make navigation to the GUI easier provide a smoother experience.
---

### Setup Instructions

Before locally running the notebook, make sure the `validation_case` is in the same directory as the notebook and is reachable. 

---

### Test Cases

All test cases are organized in the `test_cases` directory.

